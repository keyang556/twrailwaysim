# 車上廣播更新流程

廣播會改版、新站會通車，因此本專案的廣播**不是寫死在程式裡的清單**：索引由
掃描資料夾建立，把檔案放進去就生效。本文說明檔名規則、匯入流程，以及新增
一條路線要改哪裡。

目前收錄：

- **臺鐵**：縱貫線北段（基隆－竹南，36 站）與山線（造橋－彰化，23 站），124 個音檔
- **捷運**：文湖線、淡水信義線（含新北投支線）、松山新店線（含小碧潭支線）、板南線、
  三鶯線、桃園機場捷運，175 個音檔（環狀線與中和新蘆線尚無來源資料）

兩個系統的音檔放在同一個資料夾、依 `line_id` 分子目錄，線別代碼不會相撞。
捷運的部分另見〈捷運的廣播〉一節：規則比臺鐵複雜，多了往○○、宣導與各種
終點／方向版本。

---

## 檔名規則

```text
data/audio/announcements/
    variants.json                 方向版本的挑選規則（可手改）
    manifest.json                 匯入紀錄，僅供追溯，執行時不會讀
    west_north/                   資料夾名稱＝路線代碼（line_id）
        TAIPEI.next.ogg
        TAIPEI.arrive.ogg
        KEELUNG.terminus.ogg
        BADU.next.keelung.ogg
    mountain/
        TAICHUNG.next.ogg
    common/                       不屬於任何車站的廣播
        DOOR.open.emu500.ogg      開關門聲以車輛型式當版本
        DOOR.close.emu500.ogg
        DOOR.open.emu700.ogg
        DOOR.close.emu700.ogg
        DOOR.open.emu800.ogg
        DOOR.close.emu800.ogg
        DOOR.open.emu900.ogg
        DOOR.close.emu900.ogg
        DOOR.close.emu3000.ogg    只錄到關門聲
        DOOR.open.pp.ogg
        DOOR.close.pp.ogg
        DOOR.open.temu2000.ogg
        DOOR.close.temu2000.ogg
        DOOR.side.left.ogg
        DOOR.side.right.ogg
        NOTICE.do_not_board.ogg
        NOTICE.unscheduled_stop.ogg
```

檔名為 `<車站代碼>.<種類>[.<版本>].<副檔名>`。

| 種類 | 意義 | 何時播 |
|---|---|---|
| `next` | 下一站 | 列車自車站**離站之後**，內容是下一個停靠站 |
| `arrive` | 到站 | 到達停靠站**之前**（1500 公尺） |
| `terminus` | 終點 | 到達終點站之前；有這一個就不播 `arrive` |

車站代碼即 `data/stations.json` 的 `id`（例如 `TAIPEI`、`SANXINGQIAO`）。

### 不屬於任何車站的廣播

索引以車站代碼為鍵，因此車門聲這類廣播用保留字當代碼，放在 `common/`：

| 檔名 | 意義 | 何時播 |
|---|---|---|
| `DOOR.open[.<車型>]` | 開門聲 | 開門時 |
| `DOOR.close[.<車型>]` | 關門聲 | 關門時 |
| `DOOR.side.left`／`DOOR.side.right` | 開門側 | 接在到站廣播之後 |
| `NOTICE.do_not_board` | 請勿上車 | 全車對號列車開門中**持續**播放 |
| `NOTICE.unscheduled_stop` | 臨停 | 停在月台以外的地方時 |

**開關門聲每一型車不一樣**，因此以車輛型式當版本（`DOOR.open.emu900`）。版本字串
是**小寫的車輛型式代碼**，因為查詢時直接把 `spec.id` 當版本傳進去。查不到該型的
版本時退回通用的 `DOOR.open`，通用檔也沒有就不出聲——不會因為多了一型反而整個
播不出來，也不會拿別型的聲音頂替。新增一型只要把音檔放進 `common/`，程式不必改。

目前錄到的車型：

| 車輛型式 | 開門 | 關門 |
|---|---|---|
| EMU500 | ✓ | ✓ |
| EMU600 | — | — |
| EMU700 | ✓ | ✓ |
| EMU800 | ✓ | ✓ |
| EMU900 | ✓ | ✓ |
| EMU3000 | — | ✓ |
| TEMU2000（普悠瑪） | ✓ | ✓ |
| PP（推拉式自強號） | ✓ | ✓ |
| TEMU1000、DR1000、DR3100、CK | — | — |

沒有音檔的車型開關門時只有文字播報，沒有聲音；這是正常狀態，不是錯誤。

**請勿上車**要在整段開門時間裡一直播（提醒月台上沒有買這班列車車票的旅客），
因此播放器支援循環：播完自己再排一次，關門動作一開始就立即停止，不等這一輪
播完。哪些車型要播寫在 `trains.json` 的 `boarding_notice`（目前是 TEMU1000、
TEMU2000、EMU3000），不是寫死車型代碼。

**開門側**接在到站廣播之後，因此排進佇列而不是蓋掉前一則——它正是要跟著到站
廣播聽到的一句。左側或右側怎麼決定見
[`src/railway_sim/timetable/door_side.py`](../src/railway_sim/timetable/door_side.py)：
月台側別沒有公開來源，標準情況一律左側，只有判定為**待避**的車站改成右側。
副檔名可以是 `.ogg`、`.wav`、`.mp3`、`.m4a`、`.flac`、`.opus`——能不能播得出來
取決於後端，索引一律照收。

### 為什麼不用清單檔

使用者的要求是「隨時可以更新，暫時沒有廣播的不能讓程式 error」。掃描資料夾
可以同時滿足兩者：

- 丟一個檔案進去就被索引，不需要同步維護任何清單。
- 檔案不存在只是「查不到」，播文字即可，不是例外。
- 檔名看不懂就跳過並記進警告，不會中斷載入。

---

## 匯入來源資料

來源檔名是給人看的：開頭有站序編號、偶有錯字、分歧站用括號標方向。匯入工具
負責翻譯：

```bash
python -m railway_sim.audio import --dry-run "…/台鐵廣播/1縱貫北" "…/台鐵廣播/2山線"
```

確認結果後拿掉 `--dry-run` 實際匯入。**內容沒變的檔案不會重寫**，因此廣播改版
時把新檔放回來源資料夾再跑一次同一行即可，重跑是安全且便宜的。

翻譯範例：

| 來源 | 匯入後 |
|---|---|
| `1縱貫北/11台北.ogg` | `west_north/TAIPEI.next.ogg` |
| `1縱貫北/14福州到站.ogg` | `west_north/FUZHOU.arrive.ogg` |
| `1縱貫北/1基隆終點.ogg` | `west_north/KEELUNG.terminus.ogg` |
| `1縱貫北/3八堵(往基隆).ogg` | `west_north/BADU.next.keelung.ogg` |
| `2山線/15經舞蹈站.ogg` | `mountain/JINGWU.arrive.ogg` |
| `開關門/900開門.ogg` | `common/DOOR.open.emu900.ogg` |
| `開關門/普悠瑪關門.ogg` | `common/DOOR.close.temu2000.ogg` |
| `開關門/pp開門.ogg` | `common/DOOR.open.pp.ogg` |
| `0其他/左側.ogg` | `common/DOOR.side.left.ogg` |
| `5屏東縣+南迴/23枋山.ogg` | `south_link/FANGSHAN.next.ogg` |

### 已修正的來源錯字

對應規則在 [`data/audio/source_map.json`](../data/audio/source_map.json)，程式不寫死
任何站名對應。`station_aliases` 只收「來源檔名與官方站名不一致」的項目：

| 來源檔名 | 實際車站 |
|---|---|
| 福州 | 浮洲 |
| 楠樹林 | 南樹林 |
| 普興 | 埔心 |
| 三性橋 | 三姓橋 |
| 麗玲 | 栗林 |
| 南市 | 南勢 |
| 經五 | 精武 |
| 經舞蹈站 | 精武（到站） |
| 龍崗／通宵 | 龍港／通霄（海線） |
| 鄰內／明雄／後必／鄰奉迎 | 林內／民雄／後壁／林鳳營（縱貫線南段） |
| 新式／人得／男子／內為 | 新市／仁德／楠梓／內惟（縱貫線南段） |
| 大林道站 | 大林（到站） |
| 鄰落／稀釋／鄰邊 | 麟洛／西勢／林邊（屏東線） |
| 內施／內師道站／龍溪／金輪 | 內獅／內獅（到站）／瀧溪／金崙（南迴線） |
| 治學／受封／玲榮興光 | 志學／壽豐／林榮新光（臺東線） |
| 大副／復原／三名／副理／瑞河 | 大富／富源／三民／富里／瑞和（臺東線） |

「台／臺」是異體字，程式會自動正規化，不需要列在別名表裡。

### 一個資料夾橫跨兩條線

`5屏東縣+南迴` 的前二十站屬於屏東線、後十站屬於南迴線。資料夾只能對到一個
路線代碼，因此後十站用 `clip_map` 逐檔指到 `south_link/`。同一個機制也用在
「檔名本身就是編號」（`900開門`）與「不屬於任何車站」（`左側`、`臨停`、
`請勿上車`）的檔案上。

### 對不上的檔案

對不上不會讓匯入失敗，只會列在報告的「未對應」裡，其他檔案照常匯入。目前
`0其他`、`1縱貫北`、`2山線`、`3海線`、`4縱貫南`、`5屏東縣+南迴`、`6台東縣`
七個資料夾全部對得上；北迴線、宜蘭線、平溪深澳線、集集線、內灣六家線還沒有
來源資料。

---

## 新增一條路線

1. 在 `source_map.json` 的 `line_ids` 加一筆「來源資料夾名稱 → 路線代碼」。
   路線代碼要與 `data/routes.json` 的 `lines` 一致（`west_south`、`coast`、
   `yilan`…）。
2. 把來源資料夾加進匯入指令。
3. 用 `python -m railway_sim.audio check` 確認覆蓋率，它會列出**已經有部分
   廣播**的路線還缺哪幾站（整條線都還沒錄的不會逐站列出，那只是雜訊）。

新站通車時同理：只要 `stations.json` 裡有那一站，把音檔改名成
`<車站代碼>.next.ogg` 放進對應資料夾就會生效。

---

## 方向版本

八堵、七堵的「下一站」廣播有往基隆／往花蓮兩個版本，差別在於提醒旅客要不要在
本站換車，因此取決於**本班列車開往哪裡**，不是取決於車站本身。規則在
[`data/audio/announcements/variants.json`](../data/audio/announcements/variants.json)：

```json
{
  "rules": {
    "BADU": [
      { "variant": "keelung", "when_service_calls_at": ["KEELUNG"] },
      { "variant": "hualien", "when_service_calls_at": ["HUALIEN", "YILAN", "SUAO"] }
    ]
  }
}
```

依序比對，本班次的停靠表只要包含其中任一站就採用該版本。都不符合時退回沒有
方向標記的檔案；沒有的話就**不播音檔**——播錯方向比不播更糟，而文字播報照常
送出，資訊不會少。

---

## 沒有廣播的情形

以下三種都是**正常狀態**，不是故障。行前提要會說明是哪一種：

| 情形 | 行前提要顯示 |
|---|---|
| 本型車沒有廣播設備（DR1000） | `無（DR1000型柴油客車沒有車上廣播設備）` |
| 這條線還沒有音檔 | `本路線尚無廣播音檔，僅提供文字` |
| 正常 | `停靠站 14 / 17 站有廣播音檔` |

DR1000 的例外由 `data/trains.json` 的 `has_broadcast: false` 決定，不是寫死車型
代碼；這型車連廣播文字都不送出，因為那台車根本沒有播出任何東西。

---

## 播放後端

臺鐵廣播是 Ogg Vorbis。Windows 內建的播放元件（wxPython 用的 Media Foundation）
**不解這個格式**——本專案以 `wx.media.MediaCtrl` 實測過，同一個控制項載入 WAV
會回報長度，載入 Ogg 則永遠沒有 `EVT_MEDIA_LOADED`。因此播放後端依序嘗試：

1. `pygame`（`pip install railway-sim[audio]`）— 內建 SDL_mixer，可直接播 Ogg。
2. `ffplay`（`ffmpeg` 隨附，在 `PATH` 上就能用）。
3. 都沒有 → 只播文字。

一次只播一則：廣播動輒數十秒（臺北的「下一站」廣播有 46 秒），站間距離卻可能
更短，疊著播兩則都聽不清楚。新的一則會蓋掉還沒播完的舊的。

用 `--no-audio` 可以關掉聲音。依規格 §20.1「不可只靠音效表達必要資訊」，每一則
廣播都會同時送出一行文字，因此關掉聲音不會少掉任何資訊。

文字是廣播內容的**摘要**而非逐字稿：實際音檔含國語、臺語、客語與英語，逐字稿
無法由檔名得知，寫成摘要才不會虛構內容（規格 §2.3）。


---

## 捷運的廣播

捷運與臺鐵共用同一套索引與播放後端，差別在**播放時機的規則**——那些規則寫在
[`data/mrt/broadcast_rules.json`](../data/mrt/broadcast_rules.json)，由
`railway_sim.audio.mrt_broadcast` 執行，程式不寫死任何站號。

### 多出來的三種廣播

| 種類 | 檔名 | 何時播 |
|---|---|---|
| `destination` | `<終點站代碼>.destination` | 往○○。台北捷運五條高運量線在**發車之前**、列車還沒啟動時播；文湖線在**發車之後**播；三鶯線與機場捷運沒有 |
| `next` | `<車站代碼>.next` | 離站後的下一站廣播。只有三鶯線（每一站）與機場捷運（A1、A2、A3、A18，加上終點為機場第二航廈的直達車在 A13）有 |
| `notice` | `NOTICE.notice` | 宣導。在指定的兩站之間、指定的方向上播一次 |

目的地廣播以**終點站代碼**命名是關鍵：同一條線的不同營運模式（淡水－象山、
北投－大安）各有各的終點，用終點站代碼查就自動播到正確的那一則，不必為每一種
模式各寫一條規則。

### 版本怎麼挑

規則檔的每一條可以指定三種條件，全部符合才算命中，由上而下取第一個：

| 條件 | 意義 |
|---|---|
| `when_terminus` | 這班車的終點站 |
| `when_origin` | 這班車的起站 |
| `when_service_class` | 車種（機場捷運的 `express`／`commuter`） |

實際用到的規則：

- **奇岩**（`R21.arrive.terminus_next`）：終點為北投的列車播特別版，其餘播正常版
- **北投**（`R22.terminus.from_branch`）：起站為新北投的列車播支線版
- **七張**（`G03.terminus.from_branch`）：起站為小碧潭的列車播支線版
- **機場第二航廈**（`A13.next.express`、`A13.terminus.express`）：終點為此站的
  直達車才播；普通車播一般的 `A13.arrive`
- 大安、台電大樓、亞東醫院、昆陽、環北：本站為終點時播 `terminus`，否則播 `arrive`
  （這一條不需要規則，索引裡有 `terminus` 就會優先用）

宣導的兩條：淡水信義線 `R26 → R27`（竹圍到紅樹林）、松山新店線 `G10 → G09` 且
終點為 `G08`（往台電大樓過了中正紀念堂之後，往新店的不播）。

### 起播距離

捷運的到站廣播用 **400 公尺**，不是臺鐵的 1500 公尺。最短的站距只有 510 公尺
（文湖線木柵至萬芳社區），照抄臺鐵的距離會在前一站還沒開走時就播下一站的到站
廣播。

### 匯入

```bash
python -m railway_sim.audio --system mrt import --dry-run "…/mrt/廣播/淡水信義線"
```

捷運的來源檔名只有各線自己的播放序號加站名（`19-1奇岩.ogg`、`20-2北投.ogg`），
序號與播放時機的關係是每一條線各自約定的，站名也有錯字（籠埔實為龍埔、巷山實為
象山、明權西路實為民權西路、七里案實為唭哩岸、海珊實為海山、永明實為永寧、
新裝富督興實為新莊副都心、泰山桂河實為泰山貴和、領口實為林口、山埤實為山鼻、
南京三名實為南京三民、湖州實為葫洲、港前實為港墘、櫻桃福德實為鶯桃福德）。

這種資料沒有規則可循，因此
[`data/mrt/audio/source_map.json`](../data/mrt/audio/source_map.json) 用
**逐檔對照**（`clip_map`）而不是靠站名比對：

```json
"淡水信義線/19-1奇岩": "R21.arrive.terminus_next",
"淡水信義線/20-2北投": "xinbeitou/R22.terminus.from_branch"
```

值可以加上 `<線別>/` 前綴，把支線的廣播放進支線的資料夾——來源把主線與支線的
音檔混在同一個資料夾裡，目標卻應該分開。

**檔名一律用維基百科的車站代碼**（`R21`、`A14a`、`LB02`），與
`data/mrt/stations.json` 的 `id` 相同，因此播哪一站不必再查對照表。

### 已知未使用的來源檔

`淡水信義線/宣導1.ogg` 沒有指定播放時機，已匯入為 `NOTICE.notice.alt.ogg` 保存，
規則檔尚未使用。知道它該在什麼時候播之後，在 `notices` 加一條
（`"variant": "alt"`）即可，不必改程式。
